apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base
  - chaos-rbac-patch.yaml

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: multigres-chaos
