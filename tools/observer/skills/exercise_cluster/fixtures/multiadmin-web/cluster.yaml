apiVersion: multigres.com/v1alpha1
kind: MultigresCluster
metadata:
  name: multiadmin-web
  namespace: default
spec:
  pvcDeletionPolicy:
    whenDeleted: Delete
    whenScaled: Delete
  multiadminWeb:
    spec:
      replicas: 1
      resources:
        requests:
          cpu: "10m"
          memory: "32Mi"
      podLabels:
        fixture: "multiadmin-web"
  cells:
    - name: "us-east-1"
      spec:
        multigateway:
          replicas: 1
  databases:
    - name: "postgres"
      default: true
      tablegroups:
        - name: "default"
          default: true
          shards:
            - name: "0-inf"
              spec:
                multiorch:
                  replicas: 1
                pools:
                  main-rw:
                    type: readWrite
                    cells: ["us-east-1"]
                    replicasPerCell: 4
                    storage:
                      size: "5Gi"
