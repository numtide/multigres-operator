name: Build main

on:
  push:
    branches:
      - main
      - main-test

permissions:
  contents: write
  packages: write
  security-events: write

jobs:
  run:
    uses: ./.github/workflows/build-and-release.yaml
    with:
      push-container-image: true